#!/bin/sh

ioreg=`ioreg -rd1 -c IOPlatformExpertDevice`
serial=`echo "$ioreg" | awk '/IOPlatformSerialNumber/ {print $3}' | tr -d '"'`
uuid=`echo "$ioreg" | awk '/IOPlatformUUID/ { print $3; }' | tr -d '"'`

cat <<EOF
{
  "serial"    : "$serial",
  "uuid"      : "$uuid"
}
EOF
